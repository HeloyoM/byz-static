import { en } from './languages'
import { } from './languages'

const config = {
    lng: 'en',
    debug: false,

    interpolation: {
        escapeValue: false,
    },

    resources: {
        en: {
            translation: en,
        },
    },
}

export default config